# AI双周报-1688数字营销工作台

## 一、基础信息

| 提交人 | 部门 | 周期 |
| --- | --- | --- |
| 童放 | 1688 数字营销 | 第 1 期 | （7 月 31 日 – 8 月 14 日）|

> 部门为按工作台性质推断填写，如有正式名称请替换。

## 二、AI双周报内容

### 1. 应用场景

**本期真实工作任务**：搭建并持续运营「1688 数字营销工作台」——一个从飞书多维表自动同步、可视化呈现 1688 推广投放（消耗 / 成交 / ROI / 点击展现）并每日自动推送日报的数据看板。

**原来的难点**：
- 投放数据分散在飞书多维表（分总览 / 计划 / 商品三层、4 个推广计划），每天人工复制 Excel、手动汇总各计划消耗与商品 ROI，耗时且易错；
- 日报需每天手动整理后发到业务群，格式不统一、常遗漏商品级分析；
- 本地看板服务常因进程退出而打不开，运维成本高。

### 2. 怎么做的

**AI（WorkBuddy 智能体）完成的步骤**：
- 解析飞书 Base 数据结构，编写 `sync_feishu.py` 全量同步脚本（API 模式，拉取 3 张每日表 + 辅助表，生成单文件 HTML 看板所需的 `data.js` / `targets.js`）；
- 生成单文件、可离线打开的 HTML 看板（总览 / 计划 / 商品三层下钻 + 目标达成）；
- 编写 `_feishu_daily.py` 日报推送脚本：自动取前一天真实数据 → 聚合各计划消耗、商品 Top、ROI、投产诊断（高效 / 零成交 / 低 ROI / 主力占比）→ 生成飞书交互卡片 → 每日 09:30 定时推送到「十三香」业务群（chat_id `oc_f768f890674973af3671b4e60f67e68c`）；
- 编写 `dashboard_guard.js` 守护进程，自动拉起并保活本地看板服务（server.js 崩溃 3 秒重启、端口空闲无缝接管、60 秒健康巡检）。

**本人（童放）做的判断 / 修改 / 复核**：
- 确认「仅大客方案有实际消耗，其余 3 计划消耗 = 0」的数据口径，明确以商品维度为 GMV / ROI 的权威来源；
- 要求 AI 小结增加「推广商品分析」（高效品、零成交品、低 ROI 品诊断），而非仅堆数字；
- 指定日报接收群为「十三香」群（`oc_` 前缀 = 群聊，脚本按前缀自动识别 chat_id）；
- 将本地看板需求明确为「长期常驻、崩溃自愈」，而非每次手动重启。

### 3. 产出与结果

**产出了什么**：
- 一个离线可用的单文件 HTML 看板 + 飞书在线托管链接，覆盖 **26 天（7/31–8/25）、819 条商品级记录、4 个推广计划**；
- 每日 09:30 自动推送的结构化日报卡片（含各计划消耗、商品 Top、AI 小结与商品分析）；
- 守护进程保活的本地服务（http://localhost:8787），进程级崩溃可自愈。

**效率 / 质量 / 成本 / 体验变化**：
- 日报从「每天手动整理 Excel + 群发」变为「全自动推送」，单日人工耗时从约 30 分钟降到 0；
- 数据口径统一（商品层为权威），ROI / 消耗汇总不再依赖人工计算，错漏风险显著降低；
- 看板服务由「动不动打不开」变为「进程级崩溃自动恢复」，可用性提升。

#### 可验证证据
- 看板本地入口：http://localhost:8787/（另有 CloudStudio 在线链接）
- 飞书日报推送记录：每日 09:30 至「十三香」群，样例 message_id `om_x100b68da2aabbd0cc1fdcb08176dc46`（8/13 日报）
- 数据规模：**大客方案** 14 天累计消耗 **¥5,825.50**、累计成交 **¥14,903.55**（整体 ROI ≈ 2.56）；其余 3 计划（全站推店 / 精准获客 / 全站销货）暂未投放（消耗 = 0）
- 脚本资产：`sync_feishu.py`（同步）、`_feishu_daily.py`（日报推送）、`dashboard_guard.js`（进程守护），均在项目目录

### 4. 业务价值

- 投放团队每天早上第一时间看到前一天「各计划花了多少、哪些商品在产出、哪些在空耗」，决策从「凭感觉」转向「看数据」；
- 商品级 ROI 诊断帮助快速识别零成交 / 低 ROI 品，支撑选品与预算优化；
- 自动化释放每日重复整理时间，且数据一致性强，可作为团队对齐的统一口径。

### 5. 可复用资产（选填）

- **脚本**：`sync_feishu.py`（飞书 Base → 看板全量同步）、`_feishu_daily.py`（日报聚合 + 飞书卡片推送，含商品分析与 AI 小结）、`dashboard_guard.js`（Node 进程守护，可复用于任意本地服务常驻）；
- **定时任务**：每日 09:30 自动推送（已配置自动化）；
- **已沉淀 Skill**：`1688-product-analysis`（商品批量分析）、`customer-review-analysis`（评论分析）、`fubag-allocation`（福袋配搭）、`moments-copywriter`（朋友圈文案），均可复用于后续运营；
- **SOP**：飞书 Base 字段 → 看板维度的映射口径、日报卡片结构模板。

### 6、下一步动作

- 推动 8/13 之后商品层数据补全（目前商品明细仅到 8/13，8/14 起仅计划级），让日报商品分析覆盖全周期；
- 增加「周 / 月 ROI 趋势」与「计划级对比」视图；
- 评估将本地看板切换为云服务器常驻线路（122.152.204.220:8788），彻底摆脱本机进程依赖；
- 探索将日报 AI 小结接入更细的异常预警（如某商品 ROI 骤降自动 @负责人）。
